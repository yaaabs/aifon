{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "npx expo export --platform web",
        "outputDirectory": "dist"
      }
    }
  ]
}